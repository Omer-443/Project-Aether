Risky files observed in the initial commit (from the git commit output) and recommended .gitignore additions
====================================================================================================

The commit output shows these risky/non-public artifacts were added:
- backend/.env
- backend/aether-api.err.log
- backend/aether-api.log
- backend/app/__pycache__/...*.pyc
- backend/data_pipeline/__pycache__/...*.pyc
- backend/tests/__pycache__/...*.pyc
- backend/models/aether_model.pt
- data/cms_real.csv
- backend/backend/reports/data_audit_report.md (may be internal)
- frontend/.gitignore (not risky, but may be redundant/confusing)
- implementation_plan.md / MASTER_CONTEXT.md / TODO.md (already in your gitignore)

What to add (patterns) to cover them going forward
---------------------------------------------------

1) Secrets
- backend/.env
- .env anywhere

Recommended patterns:
- backend/.env
- .env
- .env.*

2) Runtime logs
- backend/aether-api.log
- backend/aether-api.err.log
- any *.log under backend

Recommended patterns:
- backend/*.log
- backend/**/*.log
- *.log

3) Pycache / bytecode
- backend/app/__pycache__/*.pyc
- backend/data_pipeline/__pycache__/*.pyc
- backend/tests/__pycache__/*.pyc

Recommended patterns:
- __pycache__/
- *.pyc

4) Model checkpoints
- backend/models/aether_model.pt

Recommended patterns:
- backend/models/
- *.pt
- *.pth
- *.ckpt
- *.onnx
- *.safetensors

5) Confidential dataset
- data/cms_real.csv

Recommended patterns:
- data/
- backend/data/

6) Internal reports/docs
- backend/backend/reports/data_audit_report.md

Recommended patterns (optional):
- backend/**/reports/
- **/data_audit_report.md


Important limitations (do not miss)
------------------------------------
- .gitignore affects only FUTURE commits.
- Already committed files remain in git history unless you rewrite history (git reset/filter-repo).

What you should do next (action plan)
--------------------------------------
1) Ensure root .gitignore contains the patterns above.
2) Then remove already-tracked risky files from the repo history (if you need them completely gone):
   - git rm --cached <path>
   - commit the removal
   - and if it was pushed publicly, run history rewrite.

